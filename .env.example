# ===========================================
# PRODUCCIÓN - api.triviachallenge.online
# ===========================================

# Server Configuration
PORT=3000
NODE_ENV=production  # ← IMPORTANTE: Cambiar a "production"

# Database (PostgreSQL en el servidor)
DATABASE_URL="postgresql://trivias_user:TU_PASSWORD_AQUI@localhost:5432/trivias_db?schema=public"

# JWT Secret (Generar uno nuevo para producción)
# Usa: openssl rand -hex 64
JWT_SECRET=GENERA_UNO_NUEVO_CON_OPENSSL_RAND_HEX_64

# CORS - URL del frontend
CORS_ORIGIN=https://app.triviachallenge.online

# API URL (para callbacks y referencias)
API_URL=https://api.triviachallenge.online

# ===========================================
# Google OAuth (Opcional - ya removido del PWA)
# ===========================================
# Si decides usar Google login en el futuro:
# GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=tu-secret
# GOOGLE_CALLBACK_URL=https://api.triviachallenge.online/api/v1/auth/google/callback

# ===========================================
# Push Notifications (VAPID Keys)
# ===========================================
# Generar con: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=tu-vapid-public-key-aqui
VAPID_PRIVATE_KEY=tu-vapid-private-key-aqui
VAPID_SUBJECT=mailto:admin@triviachallenge.online
